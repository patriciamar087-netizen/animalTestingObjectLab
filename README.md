# animalTestingObjectLab

1. Why do we use@Override with toString()?
     We use this to tell Java we are actually replacing the version that already exists in the On=bject class. W/o this we can still run the Java program but we loose safeety checks and clarity

   2. List your class's three fields and their default values before you assigned anything to them.
        my class had 3 fields :
      name (String) default value is null
      age (int) default value is 0
      color (String) default value is null
